
.row
  .span12#group-setup
    = simple_form_for @group_setup, url: finish_group_path, method: :put do |f|
      %ul.nav.nav-tabs
        %li.disabled.active
          %a{ href: "#group-tab" }
            = t("group_setup_form.tabs.group")
        %li.disabled
          %a{ href: "#discussion-tab" }
            = t("group_setup_form.tabs.discussion")
        %li.disabled
          %a{ href: "#invite-tab" }
            = t("group_setup_form.tabs.invites")
      .tab-content#wizard-tabs
        .tab-pane.active#group-tab
          %h1= t("group_setup_form.group_header")
          .row
            .span5
              .control-group
                = f.input :group_name, input_html: { class: 'validate-presence' }, label: "What is the name of your group?"
                .inline-help= t("group_setup_form.errors.group_name")
              .control-group
                = f.input :group_description, input_html: { class: 'validate-presence' }
                .inline-help= t("group_setup_form.errors.group_description")
              %h2= t("group_setup_form.group_settings_header")
            .span3.offset1.more-help
              %p
                The first question every new member will have is
                %em"what is this group for?"
              %p
                Think about the simplest way you can describe the group:
                %ul
                  %li What do you want to achieve together?
                  %li What types of decisions will you be making?
                  %li Who will be involved?
          .row
            .span5
              = f.input :viewable_by, :collection => {'Public - anyone with the link can see this group' => 'everyone', 'Members - only members of this group can see it' => 'members'}, :as => 'radio_buttons', label: "Who can see this group?"
              %br
              = f.input :members_invitable_by, :as => "radio_buttons", :collection => {'All group members' => 'members', 'Admins only' => 'admins'}, label: "Who can invite members to this group?"

        .tab-pane#discussion-tab
          .row
            .span5
              %h1= t("group_setup_form.discussion_header")
          .row
            .span5
              .control-group
                = f.input :discussion_title, input_html: { class: 'validate-presence' }
                .inline-help= t("group_setup_form.errors.discussion_title")
              .control-group
                = f.input :discussion_description, input_html: { class: 'validate-presence' }
                .inline-help= t("group_setup_form.errors.discussion_description")
            .span3.offset1.more-help
              %p Make sure you start with a clear discussion that your group members can easily engage with, so people can jump in and start participating straight away.
              %p Is your group already having a discussion that you could shift to Loomio?

        .tab-pane#invite-tab
          = render 'groups/invite_users', form: f, group: @group
      .row
        .span5
          = f.submit 'Send invites', class: 'btn btn-info btn-large run-validations', id: 'send_invites', :data => { :disable_with => "Send invites" }
          = link_to 'Next', '#', class: 'btn btn-info btn-large', id: 'next'
          = link_to 'Back', '#', class: 'btn btn-warning btn-large', id: 'prev'
      .row
        .span5#saved-at

